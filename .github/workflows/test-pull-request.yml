---
  name: Validate pull request
  
  on: pull_request

  permissions:
    contents: read
    statuses: write
  
  jobs:
    testing:
      uses: AdobeDocs/commerce-php/.github/workflows/validate-pr_job.yml@main
